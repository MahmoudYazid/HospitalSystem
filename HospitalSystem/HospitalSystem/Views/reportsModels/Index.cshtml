@model IEnumerable<HospitalSystem.Models.databaseModels.reportsModel>

@{
    ViewData["Title"] = "reports Model";
}

<h1>reports Model</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.time)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.report)
            </th>
            <th>
                Doctor Name
               
            </th>
            <th>
                patient Name
          
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.time)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.report)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.doctorModel.name)
            </td>
            <td>
                   
                        @Html.DisplayFor(modelItem => item.patientModel.Name)
                    
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
